

const defaultState = {
  current_user: null,
  errors: []
}


const SessionReducer = (state = defaultState, action) => {
  switch(action.type){
    default:
      return state
  }
}

export default SessionReducer;
